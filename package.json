{
  "name": "origami-simulator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "description": "Origami Simulator — tooling for the WebGL→WebGPU migration (Phase 0).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:regression": "playwright test",
    "regression:update": "UPDATE_SNAPSHOTS=1 playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "eslint": "^10.4.1",
    "globals": "^17.6.0",
    "vite": "^8.0.16"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
